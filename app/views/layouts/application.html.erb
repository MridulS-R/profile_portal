
<!DOCTYPE html>
<html>
  <head>
    <title><%= t("layouts.application.title") %></title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>
    <%= stylesheet_link_tag "application", "post", "background", "profile", "data-turbo-track": "reload" %>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-QWTKZyjpPEjISv5WaRU9OFeRpok6YctnYmDr5pNlyT2bRjXh0JMhjY6hW+ALEwIH" crossorigin="anonymous">
    <%= stylesheet_link_tag 'semantic.min', 'data-turbo-track': 'reload' %>
    <%= javascript_importmap_tags %>
    <style>
      <% intensity = (@user&.respond_to?(:background_intensity) && @user.background_intensity.present?) ? @user.background_intensity : 'medium' %>
      :root {
        --accent: <%= (@user&.respond_to?(:accent_color) && @user.accent_color.present?) ? @user.accent_color : '#0d6efd' %>;
        --bs-primary: var(--accent);
        --bs-link-color: var(--accent);
        /* Optional visual tuning controlled by background_intensity */
        --glass-alpha: <%= ({'soft'=>0.08,'medium'=>0.10,'bold'=>0.14}[intensity]) %>;
        --glass-blur: <%= ({'soft'=>'2px','medium'=>'3px','bold'=>'5px'}[intensity]) %>;
        --card-shadow-y: .75rem;
        --card-shadow-blur: 1.25rem;
        --card-shadow-alpha: <%= ({'soft'=>0.16,'medium'=>0.20,'bold'=>0.26}[intensity]) %>;
        --card-shadow-alpha-hover: <%= ({'soft'=>0.22,'medium'=>0.26,'bold'=>0.34}[intensity]) %>;
      }
    </style>
    <% if @user&.respond_to?(:custom_css) && @user.custom_css.present? %>
      <style><%= @user.custom_css.html_safe %></style>
    <% end %>
  </head>
  <body class="d-flex flex-column min-vh-100 abstract-bg <%= @theme == 'dark' ? 'bg-dark text-light' : 'bg-light' %>">
    <nav class="navbar navbar-expand-lg navbar-dark bg-dark">
      <div class="container">
        <%= link_to "UPLinks", root_path, class: "navbar-brand fw-semibold" %>
        <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarContent" aria-controls="navbarContent" aria-expanded="false" aria-label="Toggle navigation">
          <span class="navbar-toggler-icon"></span>
        </button>
        <div class="collapse navbar-collapse" id="navbarContent">
          <ul class="navbar-nav me-auto mb-2 mb-lg-0">
            <li class="nav-item"><%= link_to "Profile", root_path, class: ["nav-link", (current_page?(root_path) ? 'active' : nil)].compact.join(' ') %></li>
            <li class="nav-item"><%= link_to "News", news_path, class: ["nav-link", (current_page?(news_path) || controller_name == 'news' ? 'active' : nil)].compact.join(' ') %></li>
            <li class="nav-item"><%= link_to "Blog", posts_path, class: ["nav-link", (controller_name == 'posts' ? 'active' : nil)].compact.join(' ') %></li>
            <% if Rails.env.development? %>
              <li class="nav-item"><%= link_to "Sidekiq", "/sidekiq", class: "nav-link" %></li>
            <% end %>
          </ul>
          <div class="ms-auto d-flex align-items-center gap-2">
            <%# Account dropdown: avatar if signed in, else 3-dot menu %>
            <div class="dropdown">
              <% if user_signed_in? %>
                <button class="btn btn-outline-light btn-sm d-flex align-items-center gap-2 dropdown-toggle" type="button" id="accountMenu" data-bs-toggle="dropdown" aria-expanded="false">
                  <% if current_user.avatar_url.present? %>
                    <img src="<%= current_user.avatar_url %>" alt="Avatar" class="rounded-circle border" width="28" height="28">
                  <% else %>
                    <span class="fw-bold">@<%= current_user.github_username || current_user.name&.first || 'U' %></span>
                  <% end %>
                </button>
                <ul class="dropdown-menu dropdown-menu-end" aria-labelledby="accountMenu">
                  <li><%= link_to "My Profile", public_profile_path(current_user), class: "dropdown-item" %></li>
                  <li><%= link_to "Edit Profile", edit_profile_path, class: "dropdown-item" %></li>
                  <li><hr class="dropdown-divider"></li>
                  <li><%= link_to "Sync GitHub", sync_github_path, class: "dropdown-item", data: { turbo_method: :post } %></li>
                  <li><%= link_to "Logout", destroy_user_session_path, class: "dropdown-item text-danger", data: { turbo_method: :delete } %></li>
                </ul>
              <% else %>
                <button class="btn btn-outline-light btn-sm dropdown-toggle" type="button" id="guestMenu" data-bs-toggle="dropdown" aria-expanded="false" aria-label="Menu">
                  ⋯
                </button>
                <ul class="dropdown-menu dropdown-menu-end" aria-labelledby="guestMenu">
                  <li><%= link_to "Sign in", new_user_session_path, class: "dropdown-item" %></li>
                </ul>
              <% end %>
            </div>
          </div>
        </div>
      </div>
    </nav>
    <main class="container py-4 flex-grow-1">
      <% flash.each do |key, message| %>
        <% cls = case key.to_s
          when 'notice', 'success' then 'alert-success'
          when 'alert', 'error', 'danger' then 'alert-danger'
          when 'warning' then 'alert-warning'
          else 'alert-info'
        end %>
        <div class="alert <%= cls %>" role="alert"><%= message %></div>
      <% end %>
      <%= yield %>
    </main>
    <footer class="site-footer py-4 border-top">
      <div class="container d-flex flex-column flex-md-row justify-content-between align-items-center gap-2">
        <div>&copy; <%= Time.current.year %> Profile Portal • Developed by <strong>Mridul Shukla</strong></div>
        <nav class="footer-links d-flex gap-3" aria-label="Footer">
          <%= link_to "Home", root_path, class: "text-decoration-none" %>
          <%= link_to "News", news_path, class: "text-decoration-none" %>
          <%= link_to "Blog", posts_path, class: "text-decoration-none" %>
          <%= link_to "Projects", projects_path, class: "text-decoration-none" %>
        </nav>
      </div>
    </footer>
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js" integrity="sha384-YvpcrYf0tY3lHB60NNkmXc5s9fDVZLESaAA55NDzOxhy9GkcIdslK1eN7N6jIeHz" crossorigin="anonymous"></script>
    <%= javascript_include_tag 'jquery.min', 'data-turbo-track': 'reload' %>
    <%= javascript_include_tag 'semantic.min', 'data-turbo-track': 'reload' %>
    <script>
      (function(){
        function initSemantic(){
          if (window.$ && $.fn && $.fn.dropdown) {
            $('.ui.dropdown').dropdown();
          }
        }
        document.addEventListener('turbo:load', initSemantic);
        document.addEventListener('DOMContentLoaded', initSemantic);
      })();
    </script>
    <style>
      /* Hide Bootstrap caret triangle for header dropdown toggles */
      #accountMenu.dropdown-toggle::after,
      #guestMenu.dropdown-toggle::after {
        display: none !important;
      }
    </style>
  </body>
  </html>
